"""External service integrations for boxBot.

Each module wraps a third-party API behind a small async interface so that
display data sources, agent tools, and SDK modules can share one client
without duplicating auth or transport code.
"""
